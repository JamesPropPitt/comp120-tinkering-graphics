```import pygame, sys
from pygame.locals import *

pygame.init()

Width = 800
height = 600

display = pygame.display.set_mode((Width, height), 0, 32)
meme = pygame.image.load = ("memes.jpg")

while True:
    for event in pygame.event.get():
        if event.type == QUIT:
            pygame.quit()
            sys.exit()

pygame.display.update()
display.blit(meme(0,0))```
